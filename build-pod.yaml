apiVersion: v1
kind: Pod
spec:
  containers:
    - name: kaniko
      image: gcr.io/kaniko-project/executor:v0.16.0
      command:
        - cat
      imagePullPolicy: Always
      resources:
        requests:
          memory: "4Gi"
          cpu: "125m"
        limits:
          memory: "4Gi"
